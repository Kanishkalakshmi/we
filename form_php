<!DOCTYPE html>
<html>
<head>
    <title>Simple Enquiry Form</title>

    <style>

        body{
            font-family:Arial;
            text-align:center;
            margin-top:50px;
        }

        input,textarea{
            padding:10px;
            margin:10px;
            width:250px;
        }

        button{
            padding:10px 20px;
        }

        .error{
            color:red;
        }

    </style>
</head>

<body>

    <h2>Simple Enquiry Form</h2>

    <form method="post">

        <input type="text" name="name" placeholder="Enter Name"><br>

        <input type="email" name="email" placeholder="Enter Email"><br>

        <textarea name="message" placeholder="Enter Message"></textarea><br>

        <button type="submit" name="submit">Submit</button>

    </form>

    <?php

        if(isset($_POST['submit'])){

            $name=$_POST['name'];
            $email=$_POST['email'];
            $message=$_POST['message'];

            if($name=="" || $email=="" || $message==""){

                echo "<h3 class='error'>All fields are required</h3>";

            }
            else{

                echo "<h3>Form Submitted Successfully</h3>";

                echo "<p><b>Name :</b> ".$name."</p>";
                echo "<p><b>Email :</b> ".$email."</p>";
                echo "<p><b>Message :</b> ".$message."</p>";

            }

        }

    ?>

</body>
</html>
